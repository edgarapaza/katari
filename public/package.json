{
  "name": "katari",
  "version": "1.0.0",
  "description": "Katari",
  "keywords": "",
  "license": "MIT",
  "author": "Edgar apaza",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass -watch scss/app.scss -o public/css/app.css"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
